import Vasha from "../src/react-vasha-phonetic";

describe("@rakibulalam/vasha", () => {
    // it(v'needs tests');
    const v = () => "";

    test("undefined", () => {
        expect(v()).toBe("");
    });
});
